import 'package:flutter/material.dart';

class CustomColor {
  static const Color whiteColor             = Color(0xFFFFFFFF);
  static const Color primaryBackgroundColor = Color(0xFF000000);
  static const Color gray                   = Colors.grey;

  // 🎨 STEREO 98 BRAND COLORS
  static const Color accentAzzurro = Color(0xFF4EC8E8);
  static const Color accentFucsia  = Color(0xFFD85D9D);

  // Tema 1 — Scuro (nero profondo, accento teal)
  static const Color primaryColor    = Color(0xFF4EC8E8); // usato in vari widget
  static const Color primaryColorOne = Color(0xFFD85D9D);
  static const Color primaryColorTwo = Color(0xFF4EC8E8);

  static const Color darkPrimaryColor    = Color(0xFF000000);
  static const Color darkPrimaryColorOne = Color(0xFF111111);
  static const Color darkPrimaryColorTwo = Color(0xFF0D1A26);

  // Tema 2 — Vivace (ispirato al logo: azzurro scuro → viola-fucsia → blu profondo)
  static const Color vivacePrimary  = Color(0xFF0D4A5E); // azzurro scuro
  static const Color vivaceCard     = Color(0xFF5C1A4A); // fucsia scuro
  static const Color vivaceCanvas   = Color(0xFF1A0A3E); // blu-viola profondo

  // Tema 3 — Blu Notte (navy flat, elegante)
  static const Color bluNottePrimary = Color(0xFF071428);
  static const Color bluNotteCard    = Color(0xFF0A1A35);
  static const Color bluNotteCanvas  = Color(0xFF0D2040);

  // Tema 4 — Amaranto (vino/fucsia profondo, complementare al rosa logo)
  static const Color amarantoPrimary = Color(0xFF1E0714);
  static const Color amarantoCard    = Color(0xFF2D0F1E);
  static const Color amarantoCanvas  = Color(0xFF1A0A28);

  // Tema 5 — Chiaro (bianco, testo scuro)
  static const Color chiaroPrimary = Color(0xFFFFFFFF);
  static const Color chiaroCard    = Color(0xFFF5F5F5);
  static const Color chiaroCanvas  = Color(0xFFEEEEEE);
  static const Color chiaroText    = Color(0xFF1A1A1A);

  // Tema 6 — Grafite (grigio carbone elegante)
  static const Color grafitePrimary = Color(0xFF2A2A2A);
  static const Color grafiteCard    = Color(0xFF333333);
  static const Color grafiteCanvas  = Color(0xFF1E1E1E);

  // Tema 7 — Rosa Cipria (rosa caldo tenue)
  static const Color rosaCipriaPrimary = Color(0xFFFFF0F5);
  static const Color rosaCipriaCard    = Color(0xFFFDE8F0);
  static const Color rosaCipriaCanvas  = Color(0xFFF5E0EA);

  // Tema 8 — Azzurro Cielo (celeste luminoso)
  static const Color azzurroCieloPrimary = Color(0xFFF0F8FF);
  static const Color azzurroCieloCard    = Color(0xFFE8F4FD);
  static const Color azzurroCieloCanvas  = Color(0xFFDDEDF8);
}
